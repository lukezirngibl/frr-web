/**
 * @since 0.5.0
 */
import * as t from 'io-ts';
import { either } from 'fp-ts/es6/Either';
/**
 * @since 0.5.0
 */
export var BooleanFromString = new t.Type('BooleanFromString', t.boolean.is, function (u, c) {
    return either.chain(t.string.validate(u, c), function (s) {
        return s === 'true' ? t.success(true) : s === 'false' ? t.success(false) : t.failure(u, c);
    });
}, String);
