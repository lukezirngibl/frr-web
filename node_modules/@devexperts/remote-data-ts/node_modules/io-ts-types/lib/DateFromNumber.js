"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
/**
 * @since 0.5.0
 */
var t = require("io-ts");
var Either_1 = require("fp-ts/lib/Either");
/**
 * @example
 * import { DateFromNumber } from 'io-ts-types/lib/DateFromNumber'
 * import { right } from 'fp-ts/lib/Either'
 *
 * const date = new Date(1973, 10, 30)
 * const input = date.getTime()
 * assert.deepStrictEqual(DateFromNumber.decode(input), right(date))
 *
 * @since 0.5.0
 */
exports.DateFromNumber = new t.Type('DateFromNumber', function (u) { return u instanceof Date; }, function (u, c) {
    return Either_1.either.chain(t.number.validate(u, c), function (n) {
        var d = new Date(n);
        return isNaN(d.getTime()) ? t.failure(u, c) : t.success(d);
    });
}, function (a) { return a.getTime(); });
