"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DateFromUnixTime = void 0;
/**
 * @since 0.5.0
 */
var t = require("io-ts");
var Either_1 = require("fp-ts/lib/Either");
/**
 * @example
 * import { DateFromUnixTime } from 'io-ts-types/lib/DateFromUnixTime'
 * import { right } from 'fp-ts/lib/Either'
 *
 * const date = new Date(1973, 10, 30)
 * const input = date.getTime() / 1000
 * assert.deepStrictEqual(DateFromUnixTime.decode(input), right(date))
 *
 * @since 0.5.0
 */
exports.DateFromUnixTime = new t.Type('DateFromUnixTime', function (u) { return u instanceof Date; }, function (u, c) {
    return Either_1.either.chain(t.Int.validate(u, c), function (n) {
        var d = new Date(n * 1000);
        return isNaN(d.getTime()) ? t.failure(u, c) : t.success(d);
    });
}, function (a) { return a.getTime() / 1000; });
